# Core data collection, labeling, and lightweight evaluation utilities.
# This file intentionally stays small. Install the heavier Gemma/LoRA stack
# from requirements-finetune.txt when you want local model work.
openai==2.7.2
python-dotenv==1.2.1
tiktoken==0.9.0

